// src/app/admin/trust/layout.tsx
import React from 'react';

// Yeh layout file trust CMS route group ko define karta hai (sirf structure ke liye)
export default function TrustLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}