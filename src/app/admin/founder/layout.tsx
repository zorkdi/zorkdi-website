// src/app/admin/founder/layout.tsx
import React from 'react';

// Yeh layout file founder CMS route group ko define karta hai (sirf structure ke liye)
export default function FounderLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}